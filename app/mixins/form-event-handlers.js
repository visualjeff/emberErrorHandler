import Ember from 'ember';

export default Ember.Mixin.create({
  //submit: function(event) {}, //Fires after submit action handler
  
  //change: function(event) {},
  //focusIn: function(event){},
  //focusOut: function(event) {},
  //input: function(event) {}
});
