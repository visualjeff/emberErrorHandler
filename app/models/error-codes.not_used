import Ember from 'ember';
import DS from 'ember-data';

export default DS.Model.extend({
    "error-codes": DS.hasMany('error-code'),
    didLoad: function() {
        Ember.debug('model didLoad');
    },
    becameInvalid: function() {
        Ember.debug('model became Invalid');
    }

});
